services:
  nginx-proxy:
    image: nginxproxy/nginx-proxy:alpine
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - certs:/etc/nginx/certs
      - vhost:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./nginx_config/custom.conf:/etc/nginx/conf.d/custom.conf:ro
    networks:
      - app-network
    restart: always

  nginx-proxy-letsencrypt:
    image: nginxproxy/acme-companion
    container_name: nginx-proxy-letsencrypt
    depends_on:
      - nginx-proxy
    volumes:
      - certs:/etc/nginx/certs:rw
      - vhost:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - acme:/etc/acme.sh
    environment:
      DEFAULT_EMAIL: b6640583@g.sut.ac.th
      NGINX_PROXY_CONTAINER: nginx-proxy
    networks:
      - app-network
    restart: always

  # --- MinIO Storage Service ---
  minio:
    image: minio/minio
    container_name: se68_minio
    restart: always
    environment:
      MINIO_ROOT_USER: "admin"
      MINIO_ROOT_PASSWORD: "install123" # Must match Go Backend config
      VIRTUAL_HOST: storage.capstonehub.me
      VIRTUAL_PORT: 9000
      LETSENCRYPT_HOST: storage.capstonehub.me
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    expose:
      - "9000"
      - "9001"
    networks:
      - app-network

  db:
    image: postgres:15-alpine
    container_name: se68_postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - app-network

  socket:
    build: ./frontend/socket-server
    container_name: se68_socket
    restart: always
    expose:
      - "3001"
    environment:
      FRONTEND_URL: https://capstonehub.me
      VIRTUAL_HOST: socket.capstonehub.me
      VIRTUAL_PORT: 3001
      LETSENCRYPT_HOST: socket.capstonehub.me
    networks:
      - app-network

  backend:
    build: ./backend
    container_name: se68_backend
    env_file:
      - .env
    restart: always
    depends_on:
      - db
      - minio
    environment:
      DATABASE_URL: postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?sslmode=disable
      FRONTEND_URL: https://capstonehub.me
      # MinIO Config for Go
      MINIO_ENDPOINT: "minio:9000"
      MINIO_ACCESS_KEY: "admin"
      MINIO_SECRET_KEY: "password123"
      SOCKET_SERVICE_URL: "http://socket:3001"
      GIN_MODE: release
      VIRTUAL_HOST: api.capstonehub.me
      VIRTUAL_PORT: 8080
      LETSENCRYPT_HOST: api.capstonehub.me
    expose:
      - "8080"
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: https://api.capstonehub.me
        NEXT_PUBLIC_SOCKET_URL: https://socket.capstonehub.me
    container_name: se68_frontend
    restart: always
    expose:
      - "5173"
    depends_on:
      - backend
      - socket
    environment:
      VIRTUAL_HOST: capstonehub.me,www.capstonehub.me
      VIRTUAL_PORT: 5173
      LETSENCRYPT_HOST: capstonehub.me,www.capstonehub.me
    networks:
      - app-network

volumes:
  pgdata:
  certs:
  vhost:
  html:
  acme:
  minio_data: # Persists your MinIO photos

networks:
  app-network:
    driver: bridge